# Frontend proxy target for Vite dev server
# If backend runs on a different port (for example 18080), update these values.

VITE_PROXY_API_TARGET=http://127.0.0.1:8080
VITE_PROXY_WS_TARGET=ws://127.0.0.1:8080
